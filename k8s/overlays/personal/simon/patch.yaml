apiVersion: apps/v1
kind: Deployment
metadata:
  name: consumer
spec:
  template:
    spec:
      serviceAccountName: ksa-developer-simon
      containers:
        - name: consumer
          env:
            - name: GCP_PROJECT
              value: "simon-env-71bf"
            - name: PG_USER
              value: "simon"
