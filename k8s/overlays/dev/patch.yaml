apiVersion: apps/v1
kind: Deployment
metadata:
  name: consumer
spec:
  template:
    spec:
      serviceAccountName: ksa-dev
      containers:
        - name: consumer
          env:
            - name: GCP_PROJECT
              value: "dev"
            - name: PG_USER
              value: "dev"
