apiVersion: apps/v1
kind: Deployment
metadata:
  name: consumer
spec:
  selector:
    matchLabels:
      run: consumer
  replicas: 1
  template:
    metadata:
      labels:
        run: consumer
    spec:
      serviceAccountName: ksa
      containers:
      - name: consumer
        image: gcr.io/env-service-304211/env-service:b257ad9ab8fd1c344a3574d1f51c782aab0ac0d9
        envFrom:
          - configMapRef:
              name: infra-config
        resources:
          limits:
            memory: "100Mi"
            cpu: "1"
          requests:
            cpu: "0.1"
            memory: "10Mi"